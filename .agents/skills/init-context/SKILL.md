---
name: init-context
description: Triggers when the user mentions /init-context, asks to initialize the context, or requests "Lee el contexto y prepárate".
---

# Initializing Centralized Context

The `/init-context` skill has been triggered. This skill mandates that you read and internalize the main context of the workspace.

## Immediate Instructions:

1. **Read the Main Context**: Immediately use the `view_file` tool to read the file [main_context.md](file:///c:/trading/bots/pyeventbt/docs/main_context.md).
2. **Internalize Constraints**: Understand the PyEventBT core, design rules (e.g., event-driven patterns, data provider access rules, Polars dataframes), execution environments, and current active strategies.
3. **Respond to User**: Confirm that you have read and assimilated the central context. Summarize the current status briefly (as instructed by the central context rules) and stand by for further instructions regarding a specific strategy or implementation task.
